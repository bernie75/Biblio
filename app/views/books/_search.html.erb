<%= simple_form_for :search, url: books_path, method: :get, remote: true do |f| %>
  <%= f.input :title, placeholder: 'recherchez le titre de votre livre ?', label: false%>

  <%= f.submit class: 'btn btn-primary' %>
<% end %>
